// REMOVED: Firebase config removed as part of authentication cleanup
// JWT auth system is now the single authentication method
// Original implementation moved to firebase.ts.backup

export default null;