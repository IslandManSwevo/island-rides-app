// REMOVED: Redux auth slice removed as part of authentication cleanup
// AuthContext is now the single source of truth for authentication
// Original implementation moved to authSlice.ts.backup

export default null;